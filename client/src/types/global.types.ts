export type LoginValues = {
    email: string
    password: string
    rememberMe?: boolean
}

export type SignUpValues = {
    name: string
    email: string
    phoneNumber: string
    password: string
    confirmPassword: string
}